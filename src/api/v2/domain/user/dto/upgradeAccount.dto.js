export default class UpgradeAccount {
    constructor(minutesPerDay, daysPerWeek, duration) {
        this.minutesPerDay = minutesPerDay;
        this.daysPerWeek = daysPerWeek;
        this.duration = duration;
    }
}